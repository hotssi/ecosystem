% style-dictionary, component

# Install style dictionary via global npm.
npm install --global style-dictionary

# Generates a starter style dictionary, based on the supplied example type.
style-dictionary init <basic|complete>

# Builds a style dictionary package from the current directory.
style-dictionary build --config <./sd.config.js>

# Removes files and folders generated by a previously run 'build' command.
style-dictionary clean --config <./sd.config.js>
