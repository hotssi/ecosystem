% madge, media

# Installation via npm
npm -g install madge

# Install dependency for macOS
brew install graphviz || port install graphviz

# Install dependency for Ubuntu
sudo apt install graphviz

# Save graph as a SVG image (require Graphviz)
madge --image <imgname>.svg <path/to/src>

# mine: Save graph as a svg image to my local directory for image CDN.
madge --image $MY_IMGS/programming/<imgname>.svg <path/to/src>
